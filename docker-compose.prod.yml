version: "3"

services:
  nginx:
    build:
      context: .
      dockerfile: nginx/prod.Dockerfile
    ports:
      - "8080:80"
    networks:
      - internal
  php:
    build:
      context: .
      dockerfile: php/prod.Dockerfile
    networks:
      - internal

networks:
  internal:
    driver: bridge